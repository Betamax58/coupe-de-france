obstacle_range: 2.5
raytrace_range: 3.0
# footprint: [[x0, y0], [x1, y1], ... [xn, yn]]
robot_radius: 0.6
inflation_radius: 0.55

observation_sources: laser_scan_sensor point_cloud_sensor

# lidar: {sensor_frame: lidar_frame, data_type: LaserScan, topic: /lidar_scan, marking: true, clearing: true}

# filtered data in order to use laser beam at the bottom of the robot 
lidar: {sensor_frame: lidar_frame, data_type: LaserScan, topic: /filtered_scan, marking: true, clearing: true}

tof: {sensor_frame: tof_frame, data_type: PointCloud2, topic: /tof_points, marking: true, clearing: true}

# observation_sources: filtered_laser_scan
# filtered_laser_scan: 
#   data_type: LaserScan
#   topic: /filtered_scan
#   marking: true
#   clearing: true
